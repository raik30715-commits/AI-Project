services:
  - type: web
    name: ai-project-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ai_api:app --bind 0.0.0.0:$PORT
    healthCheckPath: /health
